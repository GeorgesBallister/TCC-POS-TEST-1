# Dependências do Node (Nunca envie isso, é muito pesado)
node_modules/
jspm_packages/

# Logs e arquivos de debug
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Banco de Dados Local (CSV)
# Ignoramos para que cada vez que você rodar o scraper,
# o git não fique pedindo para "commitar" as mudanças nos dados.
data/events.csv

# Arquivos de Sistema Operacional (Lixo do Windows/Mac)
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Configurações de IDE (VS Code, etc)
.idea/
.vscode/
*.swp
*.swo

# Arquivos de ambiente (Segurança: nunca suba senhas/chaves)
.env
.env.test
.env.production

# Arquivos temporários do Puppeteer (Cache do Chrome)
.puppeteer/